import document from"document";import*as screenManager from"./screen.js";import*as messaging from"messaging";let spinner=document.getElementById("spinner");spinner.state="disabled",screenManager.showHome(),document.onkeypress=function(e){"up"===e.key&&screenManager.showMenu()},messaging.peerSocket.onmessage=(e=>{spinner.state="disabled",console.log(),"number"==typeof e.data?screenManager.showResults(e.data):screenManager.showErrorScreen()});